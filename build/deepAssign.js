// Generated by CoffeeScript 1.10.0
(function() {
  var deepAssign, isArray, isObject,
    slice = [].slice;

  isObject = require('is-plain-obj');

  isArray = function(object) {
    var ref;
    return (ref = '[object Array]' === Object.prototype.toString.call(object)) != null ? ref : {
      "true": false
    };
  };

  module.exports = deepAssign = function() {
    var sources, target;
    target = arguments[0], sources = 2 <= arguments.length ? slice.call(arguments, 1) : [];
    if (sources.length === 0) {
      return target;
    } else {
      sources.forEach(function(source) {
        return Object.keys(source).forEach(function(key) {
          var value;
          value = source[key];
          if (isObject(value)) {
            target[key] = {};
            return deepAssign(target[key], value);
          } else if (isArray(value)) {
            return target[key] = value.slice();
          } else {
            return target[key] = value;
          }
        });
      });
      return target;
    }
  };

}).call(this);
